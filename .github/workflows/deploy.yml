name: Deploy to GitHub Pages

on:
  # Auto-deploy on every merge/push to main
  push:
    branches: [ main ]
  # Allow manual runs from the Actions tab
  workflow_dispatch:

# Permissions needed for the official Pages deploy actions
permissions:
  contents: read
  pages: write
  id-token: write

# Let an in-progress deploy finish; queue the latest without piling up runs
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      # No build step. Stage only the static site files (not docs/, README, etc.)
      # into a clean publish dir so nothing extra gets served.
      - name: Stage static site
        run: |
          mkdir -p _site
          cp index.html style.css _site/
          cp -r images _site/
          touch _site/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
